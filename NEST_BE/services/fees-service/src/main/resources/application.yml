server:
  port: 8087

spring:
  application:
    name: fees-service

nest:
  jwt:
    secret: ${NEST_JWT_SECRET:dev-only-change-me-dev-only-change-me-32b}
    issuer: nest

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
